image: ultainfinity-wallet-website

pipelines:
  branches: # Branch-specific pipelines definitions
    develop:
      - step:
          name: Build and test the app
          image: ultainfinity-wallet-website-develop:latest
          script:
            - npm install
            - npm run test
            - npm run build
  # default:
  # custom: